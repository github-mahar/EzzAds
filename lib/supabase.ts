// ============================================
// SUPABASE CLIENT — THE EZZADS GENERATOR
// ============================================
// Placeholder: Will be initialized when Supabase project is configured.
// Per architecture-lock.md §3: Supabase is the single source of truth.

// import { createClient } from '@supabase/supabase-js';

// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
// const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

// export const supabase = createClient(supabaseUrl, supabaseAnonKey);

export const SUPABASE_STATUS = 'PENDING_CONFIGURATION' as const;
